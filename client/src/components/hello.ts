const hello = {
    std1 : {
        isPresent : false
    },
    std2 : {
        isPresent : false
    },
    std3 : {
        isPresent : false
    }
}

const handleAttendance = (student:String)=>{
    // here I want to set the corresponding student attendance to true using react state
    // for example student argument comes as "std1" --> now your task is to put std1 : {isPresent : true}. Keep in mind that the student argument come as string format 

    // hey chatGPT write code below

    
}